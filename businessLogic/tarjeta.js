var Tarjeta  = require('../models/persona');

function authorize(numeroTarjeta,password){
    return new Promise((resolve,reject)=>{
        //check if number exist
            //check if password match
    });
}
